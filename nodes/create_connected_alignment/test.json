{
  "node": "create_connected_alignment",
  "version": 1,
  "fixture": "examples/channel-demo.dwg",
  "prerequisite": "offset_alignment (two segmented offset alignments leaving a gap at the intersection)",
  "assertions": [
    "data.length > 0 (length 0 means invalid geometry; the op already throws)",
    "data.length is close to radius x deflection angle in radians (single Arc group)",
    "dragging a parent alignment makes the connected alignment follow (GUI check)"
  ],
  "status": "awaiting_real_dwg_regression",
  "first_real_use": "Intersection corners of a secondary-channel design drawing (2026-08-23)"
}
