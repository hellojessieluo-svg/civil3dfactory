{
  "node": "dump_label_styles",
  "version": 1,
  "fixture": "examples/channel-demo.dwg",
  "assertions": [
    "data.styles_scanned > 100",
    "with contains, every style's text-component contents include the substring (note \\U+XXXX escapes: literal non-ASCII text is not found -- contents are usually <[Description]> dynamic fields or escape sequences)",
    "used_by_codes reports the CodeSetStyles branch attachments (style -> code set / code)"
  ],
  "note": "Field record (2026-08-26): 105 styles under the LabelStyles root + 15 under the CodeSetStyles branch. Three dead ends were tried: (1) deep reflection crawl (Flatten on arbitrary objects) crashes accoreconsole natively and cannot be caught; (2) crawling from the civ.Styles root crashes the same way; (3) DXFOUT does not expose label text (proxy). The only working path is strongly typed GetComponents(Text/Line/Block) plus property walking that collects ObjectIds only. It solved the 655 doubled-label mystery (@C3DF-centerline attached as origin to 8 code sets, so the same style rendered twice).",
  "status": "draft_assertions_pending_owner_review"
}
