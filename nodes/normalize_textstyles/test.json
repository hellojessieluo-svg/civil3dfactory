{
  "node": "normalize_textstyles",
  "version": 1,
  "executor": "powershell_external",
  "fixture": "examples/title-block-demo.dwg",
  "fixture_requirements": [
    "The drawing contains both text styles targeted by run.ps1: the SimHei-convention style (name '-' + U+9ED1 U+4F53) and txt1 (missing either one makes the node fail by design)",
    "At least one text references each of the two styles so the glyph change after Regen can be checked visually"
  ],
  "task": {
    "out": "${temp}/normalize-textstyles-out.dwg",
    "overwrite": true
  },
  "assertions": [
    "result.ok == true",
    "source SHA256 unchanged (the node writes only out and refuses in-place editing)",
    "out equal to dwg path fails and produces no output",
    "out already present without overwrite fails",
    "after reopening out, GetFont typeface of the SimHei-convention style == 'SimHei' (FontFile alone cannot identify a TTF)",
    "after reopening out, txt1.FontFile matches gbenor.shx and BigFontFile matches gbcbig.shx",
    "both styles have width_factor == 0.8",
    "no acad.exe process is left behind"
  ],
  "status": "moved_from_root_script_pending_fixture"
}
