{
  "node": "normalize_textstyles",
  "version": 1,
  "executor": "powershell_external",
  "fixture": "",
  "fixture_requirements": [
    "图中同时存在名为 -黑体 与 txt1 的文字样式（缺任一样式节点直接报错，这是设计行为）",
    "至少一处文字引用这两个样式，便于人眼确认 Regen 后字形变化"
  ],
  "task": {
    "out": "${temp}/normalize-textstyles-out.dwg",
    "overwrite": true
  },
  "assertions": [
    "result.ok == true",
    "源图 SHA256 不变（节点只写 out，拒绝原地改）",
    "out 与 dwg 同路径时报错并不产生输出",
    "out 已存在且未给 overwrite 时报错",
    "重开 out 后 -黑体 的 GetFont typeface == 'SimHei'（FontFile 不足以判定 TTF）",
    "重开 out 后 txt1.FontFile 命中 gbenor.shx 且 BigFontFile 命中 gbcbig.shx",
    "两个样式 width_factor == 0.8",
    "跑完没有残留 acad.exe 进程"
  ],
  "status": "moved_from_root_script_pending_fixture"
}
