{
  "node": "export_corridor_feature_lines",
  "version": 1,
  "fixture": "examples/channel-demo.dwg",
  "prerequisite": null,
  "fixture_requirements": [
    "At least one corridor whose assembly uses the shop PKT (point codes include daylight-left/right, toe-left/right)",
    "The corridor surface can be evaluated to a z range (guarantees feature line points carry real elevations)"
  ],
  "assertions": [
    "data.exported >= 1 and data.skipped explains why entries were skipped",
    "data.bytes > 0 and the out path is an openable DWG containing Polyline3d and no Civil objects",
    "items[*].z_min/z_max match the corridor surface elevation range (daylight follows terrain, toe is flat at the design bottom elevation)",
    "the codes filter applies: only the named point-code layers appear",
    "with overwrite=false an existing target must be refused",
    "items[*].points >= min_points"
  ],
  "status": "draft_assertions_pending_owner_review"
}
