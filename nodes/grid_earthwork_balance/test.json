{
  "node": "grid_earthwork_balance",
  "version": 1,
  "executor": "engine",
  "fixture": "examples/channel-demo.dwg",
  "prerequisite": "The drawing has the volume surface Surface1 (base = existing ground, comparison = design); boundaries are given as toe-line handles",
  "fixture_requirements": [
    "One TinVolumeSurface whose base and comparison surfaces both have geometry",
    "At least one closed boundary containing both cut and fill (verifies internal movement > 0)",
    "Known GetBoundedVolumes truth for the boundary (verifies closure error <= 2% and that total_tin_* matches the reference table after balancing)",
    "cut != fill (verifies shortfall_borrow / surplus_export make up exactly the difference)"
  ],
  "task": {
    "volume_surface": "Surface1",
    "handles": ["DBD3"],
    "names": { "DBD3": "I7" },
    "step": 3,
    "solver_max_nodes": 1400,
    "volume_factor": 1.06,
    "bands": [30, 50, 100, 150, 200, 300],
    "export_excel": false,
    "draw_cells": true,
    "draw_arrows": true
  },
  "expect": {
    "boundaries": 1,
    "total_tin_cut": 20381.0,
    "total_tin_fill": 38888.0,
    "total_internal_moved": 20381.0,
    "total_shortfall_borrow": 18507.0,
    "closure_note": "Measured closure error cut 0.42% / fill 0.13% (step=3); measured avg_dist_m 38.6 (+/- 2 m acceptable with a 6 m solver grid)",
    "invariants": [
      "total_internal_moved == min(total_tin_cut, total_tin_fill) +/- 1",
      "sum of histogram band volumes == internal_moved +/- 0.5",
      "cells_off_surface == 0 (when the boundary lies inside the surface)"
    ]
  },
  "verified": "2026-08-20 real drawing, region I7: 20381 m3 moved internally, mean haul 38.6 m, 56 s (6 m solver grid, 1365 flows, 60 arrows)",
  "note": "Do not substitute sample_polyline_elevations plus a home-made volume -- the volume truth must be balanced through GetBoundedVolumes; a quiet all-zero success is caught by the node's own assertions."
}
