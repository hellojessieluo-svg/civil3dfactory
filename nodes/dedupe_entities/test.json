{
  "node": "dedupe_entities",
  "version": 1,
  "fixture": "examples/channel-demo.dwg",
  "assertions": [
    "erased_duplicates matches a per-entity DXF count (a real drawing gave 1844: SECT-LABL 675 + XSEC-TABL 1075 + others)",
    "moves match count = number of sections (215 dredge control lines, one per section)",
    "after deduplication the DXF has no repeated (content, layer, position) groups"
  ],
  "note": "Field record (2026-08-26): the only cure for the section-label engine drawing everything twice on exported drawings. Warning: moves matching must first decode \\U+XXXX escapes -- MTEXT in proxy-exploded output stores non-ASCII text as escapes, so a literal IndexOf matched 0 (tripped on the first attempt). Volume tables (XSEC-TABL) are also doubled and are cleared together.",
  "status": "draft_assertions_pending_owner_review"
}
