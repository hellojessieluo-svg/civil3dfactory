{
  "node": "arrange_section_sheets",
  "version": 1,
  "fixture": "examples/channel-demo.dwg",
  "prerequisite": "create_section_views",
  "assertions": [
    "data.alignments_arranged > 0",
    "data.frames_drawn > 0",
    "data.frames_erased >= 0",
    "data.grid_per_sheet is not empty",
    "data.frame_layer is not empty"
  ],
  "note": "Assertions are structural checks drafted by the AI from the implementation's output keys; frames_drawn > 0 reflects the shop rule that sheet layouts must draw closed helper frames. Domain acceptance (sections fall inside the frames, no cross-sheet truncation) is still to be supplied by the owner.",
  "status": "draft_assertions_pending_owner_review"
}
