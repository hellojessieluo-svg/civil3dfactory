{
  "node": "annotate_closed_polyline_areas",
  "version": 1,
  "fixture": "examples/title-block-demo.dwg",
  "prerequisite": null,
  "fixture_requirements": [
    "Model space contains at least two closed LWPOLYLINEs, one of them with an arc segment",
    "At least one non-convex closed LWPOLYLINE",
    "Optional: two degenerate closed LWPOLYLINEs with coincident centroids, to verify automatic label offsetting"
  ],
  "assertions": [
    "data.polylines > 0",
    "data.labels == data.polylines",
    "data.per_polyline length == data.polylines",
    "every label_point lies inside its non-degenerate region",
    "handles listed in area_overrides report final_area equal to the override and corrected == true",
    "data.final_total_area == sum of per_polyline.final_area",
    "when export_excel == true the files in data.excel_files exist",
    "on a second run data.cleared_old == the first run's data.labels and the total label count does not grow"
  ],
  "status": "awaiting_real_dwg_regression"
}
