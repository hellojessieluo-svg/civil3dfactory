{
  "node": "annotate_grid_elevations",
  "version": 1,
  "fixture": "examples/channel-demo.dwg",
  "prerequisite": null,
  "fixture_requirements": [
    "At least two closed polylines on the boundary layer (one containing an arc segment)",
    "One design surface and one existing-ground surface covering most, but not all, of the boundary extent"
  ],
  "assertions": [
    "data.boundaries >= 2",
    "data.nodes > 0",
    "data.texts == data.nodes * 3",
    "data.grid_segments > 0",
    "data.per_boundary length == data.boundaries",
    "data.first_node_sample.diff == design - existing",
    "data.duplicate_nodes_skipped >= 0",
    "all three text pieces of every node carry XData C3DF_GRIDELEV with identical node coordinates",
    "nodes outside the surface extent are written as a dash",
    "on a second run data.cleared_old > 0 and data.nodes equals the first run",
    "the number of polylines on the boundary layer is unchanged and no text is added outside the boundaries"
  ],
  "status": "awaiting_real_dwg_regression"
}
