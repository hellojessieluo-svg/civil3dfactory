{
  "node": "create_dike_sample_lines",
  "version": 1,
  "fixture": "examples/channel-demo.dwg",
  "assertions": [
    "data.dike_count > 0",
    "data.dikes is not empty",
    "dikes[*].alignment is not empty",
    "dikes[*].lines > 0",
    "dikes[*].source_handle is not empty"
  ],
  "note": "Assertions are structural checks drafted by the AI from the implementation's output keys. Fixture still to be decided: needs a real engineering drawing with dike centreline / number / station layers. Domain acceptance (alignment direction, sample line spacing and section margins meeting sheet requirements) is still to be supplied by the owner.",
  "status": "draft_assertions_pending_owner_review"
}
