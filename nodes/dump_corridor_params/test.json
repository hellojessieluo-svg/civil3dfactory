{
  "node": "dump_corridor_params",
  "version": 1,
  "fixture": "examples/channel-demo.dwg",
  "prerequisite": null,
  "fixture_requirements": [
    "At least one corridor (with baseline and regions) and one assembly (with subassembly parameters) in the drawing",
    "The corridor baseline alignment carries a design profile"
  ],
  "assertions": [
    "data.corridor_count >= 1",
    "data.assembly_count >= 1",
    "data.corridors[0].baselines[0].regions length > 0",
    "data.assemblies[0].subassemblies[*].params numeric parameters carry value and readonly",
    "with corridors=false no corridors are returned; with assemblies=false no assemblies are returned",
    "with the assembly filter only the assembly of that name is returned"
  ],
  "status": "draft_assertions_pending_owner_review"
}
